services:
  php:
    build:
      context: .
      dockerfile: docker/Dockerfile
      args:
        USER_ID: ${UID:-1000}
        GROUP_ID: ${GID:-1000}
    volumes:
      - ./:/app
    working_dir: /app
    tty: true

  docs:
    image: node:20-alpine
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ./docs:/app
    working_dir: /app
    ports:
      - "${DOCS_PORT:-3000}:3000"
    command: sh -c "npm ci && npx docusaurus start --host 0.0.0.0"